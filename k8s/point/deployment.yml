apiVersion: apps/v1
kind: Deployment

metadata:
  name: point-service

spec:
  replicas: 2
  selector:
    matchLabels:
      app: point-service
  template:
    metadata:
      labels:
        app: point-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: point-service
          image: 267837905230.dkr.ecr.ap-northeast-2.amazonaws.com/board-system/point-service-nest:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: point-config
            - secretRef:
                name: point-secret
                